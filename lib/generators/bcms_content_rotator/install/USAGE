Description:
    Installs the bcms_content_rotator module.

Example:
    rails generate bcms_content_rotator:install

    This will:
		1. Copy any migrations from the gem into the project.
		2. Add the routes to the config/routes.rb

